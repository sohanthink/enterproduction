import type { Metadata } from "next";
import { Geist, Geist_Mono, Oswald } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const oswald = Oswald({
  variable: "--font-oswald",
  subsets: ["latin"],
  weight: ["200", "300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "enterproduction - High-End Digital Agency | Website Development & Design",
  description: "Premium digital solutions for forward-thinking brands. Expert website development, design, social media management, and marketing services. Elevate your digital presence with enterproduction.",
  keywords: "website development, web design, digital agency, social media management, marketing, website management, high-end design",
  openGraph: {
    title: "enterproduction - High-End Digital Agency",
    description: "Premium digital solutions for forward-thinking brands",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${oswald.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
